defmodule GuardianPaseto do
  @moduledoc """
  Documentation for GuardianPaseto.
  """
  alias Guardian.Token

  def build_claims(mod, resource, sub, claims, opts) do
  end

  def create_claims(mod, claims, opts) do
  end

  def decode_token(mod, token, opts) do
  end

  def exchange(mod, token, from_type, to_type, opts) do
  end

  def peek(mod, token) do
  end

  def refresh(mod, old_token, opts) do
  end

  def revoke(mod, claims, token, opts) do
  end

  def token_id() do
  end

  def verify_claims(mod, claims, opts) do
  end
end
